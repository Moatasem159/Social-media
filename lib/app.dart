import 'package:flutter/material.dart';
class Socialfy extends StatelessWidget {
  const Socialfy({super.key});
  @override
  Widget build(BuildContext context) {
    return MaterialApp.router();
  }
}